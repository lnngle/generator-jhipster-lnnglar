package cn.iocoder.yudao.module.system.dal.dataobject;

import cn.iocoder.yudao.framework.mybatis.core.dataobject.BaseDO;
import com.baomidou.mybatisplus.annotation.KeyWrapper;
import com.baomidou.mybatisplus.annotation.TableId;
import com.baomidou.mybatisplus.annotation.TableName;
import lombok.*;

import java.time.LocalDateTime;
<% if (fields.some(f => f.type === 'BigDecimal')) { %>
import java.math.BigDecimal;<% } %>
<% if (fields.some(f => f.type === 'LocalDate')) { %>
import java.time.LocalDate;<% } %>
<% if (fields.some(f => f.type === 'LocalTime')) { %>
import java.time.LocalTime;<% } %>

/**
 * <%= entityClass %> DO
 *
 * @author <%= author %>
 */
@TableName("<%= tableName %>")
@Data
@EqualsAndHashCode(callSuper = true)
@ToString(callSuper = true)
@Builder
@NoArgsConstructor
@AllArgsConstructor
public class <%= entityClass %>DO extends BaseDO {

    /**
     * ID
     */
    @TableId
    private Long id;

<% fields.forEach(function(field, index) { %>
    /**
     * <%= field.comment || field.name %>
     */
    <% if (field.required) { %>/** 必填 */<% } %>
    private <%= field.type %> <%= field.name %>;

<% }); %>
}